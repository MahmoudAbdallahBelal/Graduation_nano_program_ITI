<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="com.example.itimobiletrack.graduation_nano_program_iti.ListMemebersOfCommunity">

    <!-- TODO: Update blank fragment layout -->
    <ListView
        android:id="@+id/xListViewMemebres"
        android:layout_width="match_parent"
        android:layout_height="400dp"
          />

    <Button
        android:layout_marginLeft="30dp"
        android:layout_marginTop="440dp"
        android:text="add Memeber"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/xAddMemberButton" />

    <Button
        android:layout_marginLeft="180dp"
        android:layout_marginTop="440dp"
        android:text="Delete Memeber"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/xDeleteButton" />

</FrameLayout>
